<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GhostShare - Secure & Free Messaging</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* ===== RESET & GLOBAL STYLES ===== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #00b7ff;
            --primary-glow: #00b7ff80;
            --primary-dark: #0066ff;
            --secondary: #00fffb;
            --accent: #4dc3ff;
            --background: #000000;
            --background-alt: #0a0a2a;
            --text: #ffffff;
            --text-muted: #008cff;
            --card-bg: rgba(0, 30, 60, 0.3);
            --card-border: rgba(0, 183, 255, 0.3);
            --transition-slow: 0.5s ease;
            --transition-medium: 0.3s ease;
            --transition-fast: 0.2s ease;
            --border-radius: 12px;
        }

        body {
            background: linear-gradient(135deg, var(--background), var(--background-alt), var(--background));
            color: var(--primary);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            padding: 20px;
            text-shadow: 0 0 5px var(--primary), 0 0 10px var(--primary);
            line-height: 1.6;
            overflow-x: hidden;
            position: relative;
        }

        /* ===== CUSTOM CURSOR ===== */
        .cursor {
            position: fixed;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            background: var(--primary);
            mix-blend-mode: difference;
            pointer-events: none;
            z-index: 9999;
            transform: translate(-50%, -50%);
            opacity: 0.7;
            transition: transform 0.1s ease;
        }

        .cursor-follower {
            position: fixed;
            width: 40px;
            height: 40px;
            border: 1px solid var(--primary);
            border-radius: 50%;
            pointer-events: none;
            z-index: 9998;
            transform: translate(-50%, -50%);
            opacity: 0.4;
            transition: all 0.6s ease;
        }

        /* ===== BACKGROUND EFFECTS ===== */
        .bg-wrapper {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -10;
            overflow: hidden;
        }

        .bg-particle {
            position: absolute;
            border-radius: 50%;
            background: var(--primary-glow);
            box-shadow: 0 0 50px var(--primary-glow);
            opacity: 0.1;
        }

        .bg-particle:nth-child(1) {
            width: 300px;
            height: 300px;
            top: -150px;
            left: -150px;
            animation: float-3d 25s infinite ease-in-out;
        }

        .bg-particle:nth-child(2) {
            width: 200px;
            height: 200px;
            bottom: -100px;
            right: -100px;
            animation: float-3d 20s infinite ease-in-out reverse;
        }

        .bg-particle:nth-child(3) {
            width: 150px;
            height: 150px;
            top: 20%;
            right: 10%;
            animation: float-3d 30s infinite ease-in-out 5s;
        }

        .bg-particle:nth-child(4) {
            width: 180px;
            height: 180px;
            bottom: 30%;
            left: 15%;
            animation: float-3d 22s infinite ease-in-out 7s;
        }

        /* ===== GRID BACKGROUND ===== */
        .grid-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(rgba(0, 183, 255, 0.05) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 183, 255, 0.05) 1px, transparent 1px);
            background-size: 40px 40px;
            z-index: -11;
            perspective: 1000px;
            transform-style: preserve-3d;
            animation: grid-move 20s infinite linear;
        }

        /* ===== CONTAINER & LAYOUT ===== */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            position: relative;
            z-index: 1;
        }

        /* ===== HEADER STYLES ===== */
        header {
            text-align: center;
            padding: 60px 0;
            position: relative;
            perspective: 1000px;
        }

        .logo-container {
            display: flex;
            justify-content: center;
            margin-bottom: 30px;
            transform-style: preserve-3d;
        }

        .logo {
            font-size: 6rem;
            margin-bottom: 15px;
            display: inline-block;
            animation: 
                pulse-glow 6s infinite alternate, 
                float-ghost 8s infinite ease-in-out,
                logo-rotate-3d 20s infinite linear;
            filter: drop-shadow(0 0 15px var(--primary));
            transform-style: preserve-3d;
            text-shadow: 0 0 10px var(--primary), 0 0 20px var(--primary), 0 0 30px var(--primary);
            position: relative;
        }

        .logo::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle, var(--primary) 0%, transparent 70%);
            opacity: 0.3;
            border-radius: 50%;
            animation: pulse-radiate 4s infinite alternate;
        }

        h1 {
            font-size: 3.5rem;
            margin-bottom: 20px;
            letter-spacing: 3px;
            background: linear-gradient(45deg, var(--primary), var(--secondary), var(--primary));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-shadow: 0 0 15px rgba(0, 183, 255, 0.5);
            animation: 
                title-glitch 10s infinite alternate,
                title-glide 4s infinite alternate;
            font-weight: 800;
            position: relative;
            display: inline-block;
        }

        h1::before, h1::after {
            content: 'GhostShare';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0.8;
            mix-blend-mode: screen;
        }

        h1::before {
            color: var(--secondary);
            animation: glitch-anim-1 5s infinite linear alternate-reverse;
            clip-path: polygon(0 0, 100% 0, 100% 35%, 0 35%);
        }

        h1::after {
            color: #ff00dd;
            animation: glitch-anim-2 7s infinite linear alternate-reverse;
            clip-path: polygon(0 65%, 100% 65%, 100% 100%, 0 100%);
        }

        .tagline {
            font-size: 1.8rem;
            margin-bottom: 50px;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
            position: relative;
            padding: 20px;
            border: 1px solid var(--card-border);
            border-radius: var(--border-radius);
            background: var(--card-bg);
            box-shadow: 
                0 0 25px var(--primary-glow),
                inset 0 0 15px var(--primary-glow);
            animation: 
                fade-in-up 1.5s ease-out,
                glow-pulse 3s infinite alternate;
            backdrop-filter: blur(10px);
        }

        /* ===== CONTENT SECTIONS ===== */
        .content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 50px;
            margin-bottom: 80px;
            animation: fade-in 1.8s ease-out;
        }

        @media (max-width: 900px) {
            .content {
                grid-template-columns: 1fr;
            }
            
            h1 {
                font-size: 2.5rem;
            }
            
            .tagline {
                font-size: 1.4rem;
            }
        }

        .description {
            background: var(--card-bg);
            padding: 40px;
            border-radius: var(--border-radius);
            border: 1px solid var(--primary);
            box-shadow: 
                0 0 30px var(--primary-glow),
                inset 0 0 20px var(--primary-glow);
            position: relative;
            overflow: hidden;
            transition: all var(--transition-medium);
            backdrop-filter: blur(10px);
            transform-style: preserve-3d;
            animation: slide-in-left 1.2s ease-out;
        }

        .description:hover {
            transform: translateY(-10px) rotateX(5deg) rotateY(-5deg) scale(1.02);
            box-shadow: 
                0 0 40px var(--primary-glow),
                inset 0 0 25px var(--primary-glow);
        }

        .description::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, var(--primary-glow), transparent);
            transition: left var(--transition-slow);
        }

        .description:hover::before {
            left: 100%;
        }

        .description p {
            font-size: 1.3rem;
            margin-bottom: 25px;
            position: relative;
            z-index: 1;
            line-height: 1.8;
        }

        .features {
            background: var(--card-bg);
            padding: 40px;
            border-radius: var(--border-radius);
            border: 1px solid var(--primary);
            box-shadow: 
                0 0 30px var(--primary-glow),
                inset 0 0 20px var(--primary-glow);
            transition: all var(--transition-medium);
            backdrop-filter: blur(10px);
            transform-style: preserve-3d;
            animation: slide-in-right 1.2s ease-out;
        }

        .features:hover {
            transform: translateY(-10px) rotateX(5deg) rotateY(5deg) scale(1.02);
            box-shadow: 
                0 0 40px var(--primary-glow),
                inset 0 0 25px var(--primary-glow);
        }

        .features h2 {
            font-size: 2.5rem;
            margin-bottom: 30px;
            text-align: center;
            color: var(--secondary);
            text-shadow: 0 0 15px var(--primary);
            animation: glow-pulse 2s infinite alternate;
            position: relative;
        }

        .features h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 3px;
            background: var(--primary);
            border-radius: 2px;
            box-shadow: 0 0 10px var(--primary);
        }

        .feature-list {
            list-style: none;
        }

        .feature-list li {
            padding: 20px;
            margin-bottom: 20px;
            background: rgba(0, 50, 100, 0.2);
            border-left: 4px solid var(--primary);
            border-radius: 8px;
            font-size: 1.2rem;
            display: flex;
            align-items: center;
            transition: all var(--transition-medium);
            animation: fade-in 1s ease-out;
            animation-fill-mode: both;
            backdrop-filter: blur(5px);
        }

        .feature-list li:nth-child(1) { animation-delay: 0.1s; }
        .feature-list li:nth-child(2) { animation-delay: 0.2s; }
        .feature-list li:nth-child(3) { animation-delay: 0.3s; }
        .feature-list li:nth-child(4) { animation-delay: 0.4s; }
        .feature-list li:nth-child(5) { animation-delay: 0.5s; }
        .feature-list li:nth-child(6) { animation-delay: 0.6s; }

        .feature-list li:hover {
            transform: translateX(15px) scale(1.05);
            box-shadow: 0 0 25px var(--primary-glow);
            background: rgba(0, 70, 140, 0.3);
        }

        .feature-list li i {
            margin-right: 20px;
            font-size: 1.5rem;
            color: var(--secondary);
            text-shadow: 0 0 10px var(--primary);
            transition: all var(--transition-medium);
            width: 30px;
            text-align: center;
        }

        .feature-list li:hover i {
            transform: scale(1.5) rotate(15deg);
            color: var(--primary);
        }

        /* ===== DOWNLOADS SECTION ===== */
        .downloads {
            text-align: center;
            margin-bottom: 80px;
            animation: fade-in-up 1.5s ease-out;
        }

        .downloads h2 {
            font-size: 2.8rem;
            margin-bottom: 40px;
            color: var(--secondary);
            text-shadow: 0 0 20px var(--primary);
            position: relative;
            display: inline-block;
            padding: 0 20px;
            animation: bounce-soft 3s infinite alternate;
        }

        .downloads h2::before, .downloads h2::after {
            content: '';
            position: absolute;
            top: 50%;
            width: 60px;
            height: 3px;
            background: linear-gradient(90deg, transparent, var(--primary));
            border-radius: 2px;
            box-shadow: 0 0 10px var(--primary);
        }

        .downloads h2::before {
            left: -70px;
            animation: line-grow-1 2s infinite alternate;
        }

        .downloads h2::after {
            right: -70px;
            background: linear-gradient(90deg, var(--primary), transparent);
            animation: line-grow-2 2s infinite alternate;
        }

        .buttons {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 25px;
            perspective: 1000px;
        }

        .button {
            padding: 22px 40px;
            background: transparent;
            color: var(--primary);
            border: 2px solid var(--primary);
            border-radius: var(--border-radius);
            font-size: 1.3rem;
            font-weight: bold;
            cursor: pointer;
            box-shadow: 
                0 0 20px var(--primary-glow),
                inset 0 0 15px var(--primary-glow),
                0 0 40px var(--primary-glow);
            text-shadow: 0 0 10px var(--primary);
            transition: all var(--transition-medium);
            min-width: 240px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
            position: relative;
            overflow: hidden;
            z-index: 1;
            transform-style: preserve-3d;
            animation: fade-in-up 1s ease-out;
            animation-fill-mode: both;
            backdrop-filter: blur(5px);
        }

        .button:nth-child(1) { animation-delay: 0.1s; }
        .button:nth-child(2) { animation-delay: 0.2s; }
        .button:nth-child(3) { animation-delay: 0.3s; }
        .button:nth-child(4) { animation-delay: 0.4s; }

        .button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, var(--primary-glow), transparent);
            transition: left var(--transition-slow);
            z-index: -1;
        }

        .button:hover {
            background-color: rgba(0, 183, 255, 0.15);
            transform: translateY(-8px) rotateX(10deg) scale(1.05);
            box-shadow: 
                0 0 30px var(--primary-glow),
                inset 0 0 20px var(--primary-glow),
                0 0 60px var(--primary-glow);
            animation: none;
        }

        .button:hover::before {
            left: 100%;
        }

        .button i {
            font-size: 1.5rem;
            filter: drop-shadow(0 0 8px var(--primary));
            transition: all var(--transition-medium);
        }

        .button:hover i {
            transform: scale(1.3) rotate(360deg);
        }

        /* ===== FOOTER STYLES ===== */
        footer {
            text-align: center;
            padding: 60px 0;
            border-top: 1px solid rgba(0, 59, 255, 0.3);
            margin-top: 60px;
            position: relative;
            animation: fade-in 2s ease-out;
        }

        footer::before {
            content: '';
            position: absolute;
            top: -1px;
            left: 50%;
            transform: translateX(-50%);
            width: 80%;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--primary-dark), transparent);
            box-shadow: 0 0 15px var(--primary-dark);
        }

        .social-links {
            margin-bottom: 40px;
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 25px;
        }

        .social-links a {
            display: inline-flex;
            align-items: center;
            color: var(--accent);
            margin: 0;
            font-size: 1.3rem;
            text-decoration: none;
            transition: all var(--transition-medium);
            padding: 15px 25px;
            border: 1px solid var(--card-border);
            border-radius: var(--border-radius);
            background: var(--card-bg);
            gap: 12px;
            animation: fade-in 1s ease-out;
            animation-fill-mode: both;
            backdrop-filter: blur(5px);
        }

        .social-links a:nth-child(1) { animation-delay: 0.1s; }
        .social-links a:nth-child(2) { animation-delay: 0.2s; }
        .social-links a:nth-child(3) { animation-delay: 0.3s; }

        .social-links a:hover {
            color: var(--primary);
            text-shadow: 0 0 20px var(--primary);
            transform: translateY(-5px) scale(1.1);
            box-shadow: 0 0 20px var(--primary-glow);
            border-color: var(--primary);
        }

        .social-links a i {
            font-size: 1.4rem;
            transition: transform var(--transition-medium);
        }

        .social-links a:hover i {
            transform: scale(1.3) rotate(10deg);
        }

        .credits {
            font-size: 1.2rem;
            margin-bottom: 25px;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 15px;
            animation: fade-in-up 1.5s ease-out;
        }

        .credits p {
            padding: 12px 20px;
            background: var(--card-bg);
            border-radius: var(--border-radius);
            border: 1px solid var(--card-border);
            transition: all var(--transition-medium);
            backdrop-filter: blur(5px);
        }

        .credits p:hover {
            transform: scale(1.05);
            box-shadow: 0 0 20px var(--primary-glow);
        }

        .code-info {
            display: flex;
            gap: 30px;
            flex-wrap: wrap;
            justify-content: center;
            margin-top: 20px;
        }

        .code-info p {
            animation: fade-in 1s ease-out;
            position: relative;
            overflow: hidden;
        }

        .code-info p::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, var(--primary-glow), transparent);
            transition: left var(--transition-slow);
        }

        .code-info p:hover::before {
            left: 100%;
        }

        .code-info p:nth-child(1) { animation-delay: 0.1s; }
        .code-info p:nth-child(2) { animation-delay: 0.2s; }

        .copyright {
            margin-top: 40px;
            font-size: 1.1rem;
            color: var(--text-muted);
            padding: 15px 25px;
            background: var(--card-bg);
            border-radius: var(--border-radius);
            display: inline-block;
            animation: fade-in 2s ease-out;
            backdrop-filter: blur(5px);
            border: 1px solid var(--card-border);
        }

        /* ===== PARTICLE EFFECT ===== */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -5;
        }

        .particle {
            position: absolute;
            width: 3px;
            height: 3px;
            background: var(--primary);
            border-radius: 50%;
            animation: particle-float 20s infinite linear;
            box-shadow: 0 0 10px var(--primary), 0 0 20px var(--primary);
        }

        /* ===== FLOATING ELEMENTS ===== */
        .floating {
            position: absolute;
            width: 25px;
            height: 25px;
            border-radius: 50%;
            background: var(--primary-glow);
            box-shadow: 0 0 15px var(--primary-glow);
            animation: float-around 25s infinite linear;
            z-index: -4;
        }

        .floating:nth-child(1) {
            top: 20%;
            left: 10%;
            animation-delay: 0s;
            animation-duration: 30s;
        }

        .floating:nth-child(2) {
            top: 60%;
            right: 15%;
            animation-delay: 5s;
            animation-duration: 25s;
        }

        .floating:nth-child(3) {
            bottom: 30%;
            left: 20%;
            animation-delay: 10s;
            animation-duration: 35s;
        }

        .floating:nth-child(4) {
            top: 40%;
            right: 25%;
            animation-delay: 15s;
            animation-duration: 28s;
        }

        /* ===== ANIMATION KEYFRAMES ===== */
        @keyframes float-3d {
            0%, 100% { 
                transform: translate3d(0, 0, 0) rotate3d(1, 1, 1, 0deg); 
            }
            25% { 
                transform: translate3d(50px, -30px, 50px) rotate3d(1, 0, 0, 90deg); 
            }
            50% { 
                transform: translate3d(0, -60px, 100px) rotate3d(0, 1, 0, 180deg); 
            }
            75% { 
                transform: translate3d(-50px, -30px, 50px) rotate3d(0, 0, 1, 270deg); 
            }
        }

        @keyframes grid-move {
            0% {
                background-position: 0 0;
                transform: rotateX(75deg) translateY(0) translateZ(0);
            }
            100% {
                background-position: 0 40px;
                transform: rotateX(75deg) translateY(-40px) translateZ(0);
            }
        }

        @keyframes pulse-glow {
            0% { 
                text-shadow: 0 0 5px var(--primary), 0 0 10px var(--primary);
                filter: drop-shadow(0 0 5px var(--primary));
            }
            100% { 
                text-shadow: 0 0 20px var(--primary), 0 0 40px var(--primary), 0 0 60px var(--primary);
                filter: drop-shadow(0 0 15px var(--primary));
            }
        }

        @keyframes float-ghost {
            0%, 100% { 
                transform: translateY(0) rotate(0deg) translateZ(0); 
            }
            50% { 
                transform: translateY(-20px) rotate(5deg) translateZ(20px); 
            }
        }

        @keyframes logo-rotate-3d {
            0% { 
                transform: rotateY(0deg) rotateX(0deg); 
            }
            100% { 
                transform: rotateY(360deg) rotateX(360deg); 
            }
        }

        @keyframes pulse-radiate {
            0% {
                opacity: 0.3;
                transform: scale(1);
            }
            100% {
                opacity: 0.6;
                transform: scale(1.5);
            }
        }

        @keyframes title-glitch {
            0%, 100% {
                transform: translate(0);
            }
            5% {
                transform: translate(2px, -2px);
            }
            10% {
                transform: translate(-2px, 2px);
            }
            15% {
                transform: translate(2px, 2px);
            }
            20% {
                transform: translate(-2px, -2px);
            }
            50% {
                transform: translate(0);
            }
        }

        @keyframes title-glide {
            0% {
                background-position: 0% 50%;
            }
            100% {
                background-position: 100% 50%;
            }
        }

        @keyframes glitch-anim-1 {
            0% {
                transform: translate(0);
            }
            20% {
                transform: translate(-5px, 3px);
            }
            40% {
                transform: translate(-5px, -3px);
            }
            60% {
                transform: translate(5px, 3px);
            }
            80% {
                transform: translate(5px, -3px);
            }
            100% {
                transform: translate(0);
            }
        }

        @keyframes glitch-anim-2 {
            0% {
                transform: translate(0);
            }
            20% {
                transform: translate(3px, -5px);
            }
            40% {
                transform: translate(-3px, -5px);
            }
            60% {
                transform: translate(-3px, 5px);
            }
            80% {
                transform: translate(3px, 5px);
            }
            100% {
                transform: translate(0);
            }
        }

        @keyframes fade-in-up {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes glow-pulse {
            from {
                box-shadow: 0 0 20px var(--primary-glow);
            }
            to {
                box-shadow: 0 0 40px var(--primary-glow);
            }
        }

        @keyframes slide-in-left {
            from {
                opacity: 0;
                transform: translateX(-100px) rotateY(90deg);
            }
            to {
                opacity: 1;
                transform: translateX(0) rotateY(0);
            }
        }

        @keyframes slide-in-right {
            from {
                opacity: 0;
                transform: translateX(100px) rotateY(-90deg);
            }
            to {
                opacity: 1;
                transform: translateX(0) rotateY(0);
            }
        }

        @keyframes fade-in {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes bounce-soft {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-15px);
            }
        }

        @keyframes line-grow-1 {
            0% {
                width: 0;
                left: -35px;
            }
            100% {
                width: 60px;
                left: -70px;
            }
        }

        @keyframes line-grow-2 {
            0% {
                width: 0;
                right: -35px;
            }
            100% {
                width: 60px;
                right: -70px;
            }
        }

        @keyframes particle-float {
            0% {
                transform: translateY(0) translateX(0) translateZ(0);
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                transform: translateY(-100vh) translateX(100px) translateZ(100px);
                opacity: 0;
            }
        }

        @keyframes float-around {
            0% {
                transform: translate(0, 0) rotate(0deg) scale(1);
            }
            25% {
                transform: translate(100px, -50px) rotate(90deg) scale(1.2);
            }
            50% {
                transform: translate(50px, -100px) rotate(180deg) scale(1);
            }
            75% {
                transform: translate(-50px, -50px) rotate(270deg) scale(1.2);
            }
            100% {
                transform: translate(0, 0) rotate(360deg) scale(1);
            }
        }

        /* ===== RESPONSIVE DESIGN ===== */
        @media (max-width: 768px) {
            h1 {
                font-size: 2.2rem;
            }
            
            .tagline {
                font-size: 1.2rem;
                padding: 15px;
            }
            
            .description, .features {
                padding: 25px;
            }
            
            .button {
                padding: 18px 25px;
                min-width: 200px;
                font-size: 1.1rem;
            }
            
            .social-links a {
                padding: 12px 20px;
                font-size: 1.1rem;
            }
            
            .downloads h2::before,
            .downloads h2::after {
                display: none;
            }
        }

        @media (max-width: 480px) {
            .logo {
                font-size: 4rem;
            }
            
            h1 {
                font-size: 1.8rem;
            }
            
            .tagline {
                font-size: 1rem;
            }
            
            .description p, .feature-list li {
                font-size: 1rem;
            }
            
            .button {
                padding: 15px 20px;
                min-width: 180px;
                font-size: 1rem;
            }
            
            .social-links {
                flex-direction: column;
                align-items: center;
            }
            
            .code-info {
                flex-direction: column;
                gap: 15px;
            }
        }

        /* ===== SCROLLBAR STYLING ===== */
        ::-webkit-scrollbar {
            width: 10px;
        }

        ::-webkit-scrollbar-track {
            background: var(--background);
        }

        ::-webkit-scrollbar-thumb {
            background: var(--primary);
            border-radius: 5px;
            box-shadow: 0 0 10px var(--primary);
        }

        ::-webkit-scrollbar-thumb:hover {
            background: var(--secondary);
        }
    </style>
</head>
<body>
    <!-- Custom Cursor -->
    <div class="cursor"></div>
    <div class="cursor-follower"></div>

    <!-- Background Effects -->
    <div class="bg-wrapper">
        <div class="bg-particle"></div>
        <div class="bg-particle"></div>
        <div class="bg-particle"></div>
        <div class="bg-particle"></div>
    </div>
    
    <!-- Grid Background -->
    <div class="grid-bg"></div>
    
    <!-- Floating elements -->
    <div class="floating"></div>
    <div class="floating"></div>
    <div class="floating"></div>
    <div class="floating"></div>
    
    <!-- Particles -->
    <div class="particles" id="particles"></div>
    
    <div class="container">
        <header>
            <div class="logo-container">
                <div class="logo">ðŸ‘»</div>
            </div>
            <h1>GhostShare</h1>
            <p class="tagline">Open-source, free, and 100% secure messaging</p>
        </header>
        
        <div class="content">
            <div class="description">
                <p>GhostShare is an open-source, free and 100% secure version of texting apps. Unlike other apps, we don't sell information, store it or grab it.</p>
                <p>Our mission is to provide truly private communication for everyone without hidden costs or data collection. Built with security and transparency in mind.</p>
            </div>
            
            <div class="features">
                <h2>Why Choose GhostShare?</h2>
                <ul class="feature-list">
                    <li><i class="fas fa-lock"></i> End-to-end encryption</li>
                    <li><i class="fas fa-code"></i> Open-source transparency</li>
                    <li><i class="fas fa-dollar-sign"></i> Completely free forever</li>
                    <li><i class="fas fa-shield-alt"></i> No data collection</li>
                    <li><i class="fas fa-server"></i> No central servers</li>
                    <li><i class="fas fa-users"></i> Community driven</li>
                </ul>
            </div>
        </div>
        
        <div class="downloads">
            <h2>Download GhostShare</h2>
            <div class="buttons">
                <button class="button" onclick="alert('Linux version is not available yet')"><i class="fab fa-linux"></i> Install for Linux</button>
                <button class="button" onclick="alert('Windows version is not available yet')"><i class="fab fa-windows"></i> Install for Windows</button>
                <button class="button" onclick="alert('MacOS version is not available yet')"><i class="fab fa-apple"></i> Install for MacOS</button>
                <button class="button" onclick="alert('Android version is not available yet')"><i class="fab fa-android"></i> Install for Android</button>
            </div>
        </div>
        
        <footer>
            <div class="social-links">
                <a href="https://discord.gg/jrVVgxAanc" target="_blank"><i class="fab fa-discord"></i> Discord Server</a>
                <a href="mailto:ghostshare25@proton.me"><i class="fas fa-envelope"></i> ghostshare25@proton.me</a>
                <a href="https://www.tiktok.com/@ghostshare.open.s?_t=ZN-8zEtohS1dPK&_r=1" target="_blank"><i class="fab fa-tiktok"></i> TikTok Account</a>
            </div>
            
            <div class="credits">
                <p>App created by MaDrug?</p>
                <p>Site created by litaliano00</p>
                <div class="code-info">
                    <p>App coded with HTML and Python</p>
                    <p>Website coded with HTML and CSS</p>
                </div>
            </div>
            
            <div class="copyright">
                <p>&copy; 2025 GhostShare. All rights reserved.</p>
            </div>
        </footer>
    </div>

    <script>
        // Wait for DOM to fully load
        document.addEventListener('DOMContentLoaded', function() {
            // Custom cursor
            const cursor = document.querySelector('.cursor');
            const cursorFollower = document.querySelector('.cursor-follower');
            
            document.addEventListener('mousemove', function(e) {
                cursor.style.left = e.clientX + 'px';
                cursor.style.top = e.clientY + 'px';
                
                setTimeout(function() {
                    cursorFollower.style.left = e.clientX + 'px';
                    cursorFollower.style.top = e.clientY + 'px';
                }, 100);
            });
            
            // Interactive cursor effects
            const interactiveElements = document.querySelectorAll('button, a, .feature-list li');
            
            interactiveElements.forEach(element => {
                element.addEventListener('mouseenter', () => {
                    cursor.style.transform = 'translate(-50%, -50%) scale(1.5)';
                    cursorFollower.style.transform = 'translate(-50%, -50%) scale(1.8)';
                    cursorFollower.style.borderWidth = '2px';
                });
                
                element.addEventListener('mouseleave', () => {
                    cursor.style.transform = 'translate(-50%, -50%) scale(1)';
                    cursorFollower.style.transform = 'translate(-50%, -50%) scale(1)';
                    cursorFollower.style.borderWidth = '1px';
                });
            });
            
            // Button animations
            const buttons = document.querySelectorAll('.button');
            buttons.forEach((button, index) => {
                button.style.animation = `none`;
                setTimeout(() => {
                    button.style.animation = `pulse-glow 2s infinite alternate`;
                    button.style.animationDelay = `${index * 0.3}s`;
                }, 500);
            });
            
            // Create particles
            const particlesContainer = document.getElementById('particles');
            const particleCount = 100;
            
            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.classList.add('particle');
                
                // Random position
                const posX = Math.random() * 100;
                const posY = Math.random() * 100;
                
                particle.style.left = `${posX}vw`;
                particle.style.top = `${posY}vh`;
                
                // Random size
                const size = Math.random() * 4 + 1;
                particle.style.width = `${size}px`;
                particle.style.height = `${size}px`;
                
                // Random animation duration
                const duration = Math.random() * 15 + 10;
                particle.style.animationDuration = `${duration}s`;
                
                // Random delay
                const delay = Math.random() * 10;
                particle.style.animationDelay = `-${delay}s`;
                
                // Random opacity
                const opacity = Math.random() * 0.7 + 0.3;
                particle.style.opacity = opacity;
                
                particlesContainer.appendChild(particle);
            }
            
            // Add typing effect to tagline
            const tagline = document.querySelector('.tagline');
            const originalText = tagline.textContent;
            tagline.textContent = '';
            let i = 0;
            
            function typeWriter() {
                if (i < originalText.length) {
                    tagline.textContent += originalText.charAt(i);
                    i++;
                    setTimeout(typeWriter, 50);
                }
            }
            
            setTimeout(typeWriter, 1000);
            
            // Parallax effect for background elements
            document.addEventListener('mousemove', function(e) {
                const x = (window.innerWidth / 2 - e.clientX) / 25;
                const y = (window.innerHeight / 2 - e.clientY) / 25;
                
                document.querySelector('.grid-bg').style.transform = `
                    rotateX(${y}deg) 
                    rotateY(${x}deg) 
                    translateY(${y}px) 
                    translateX(${x}px)
                `;
                
                const particles = document.querySelectorAll('.bg-particle');
                particles.forEach(particle => {
                    const speed = parseInt(particle.style.width) / 50;
                    particle.style.transform = `
                        translateX(${x * speed}px)
                        translateY(${y * speed}px)
                    `;
                });
            });
            
            // Add scroll animations
            const observerOptions = {
                root: null,
                rootMargin: '0px',
                threshold: 0.1
            };
            
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.animationPlayState = 'running';
                        observer.unobserve(entry.target);
                    }
                });
            }, observerOptions);
            
            // Observe all animated elements
            document.querySelectorAll('[class*="animation"]').forEach(el => {
                observer.observe(el);
            });
        });
    </script>
</body>
</html>
